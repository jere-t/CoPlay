// store/reducers/club.js

const initState = {
  activeClubId: 1,
}

const club  = (state = initState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default club;
